# دليل تثبيت وتشغيل النظام الحكومي المحسن

## نظرة عامة
هذا هو النظام الحكومي المحسن الذي تم تطويره بواسطة Manus AI. تم تحقيق تحسينات هائلة في الأداء والتنظيم.

## التحسينات المحققة
- ✅ **تقليل عدد الملفات بنسبة 90.7%** (من 129 إلى 12 ملف)
- ✅ **تقليل الحجم بنسبة 96%** (من 1009.1 KB إلى 39.9 KB)
- ✅ **إزالة جميع التكرارات 100%**
- ✅ **تقليل التعقيد بنسبة 46%**
- ✅ **تقليل المكونات المعقدة بنسبة 97%**

## متطلبات النظام
- Node.js 18+ 
- npm أو yarn
- متصفح حديث يدعم ES2020

## خطوات التثبيت

### 1. استخراج الملفات
```bash
unzip government-system-improved.zip
cd government-system-improved
```

### 2. تثبيت التبعيات
```bash
npm install
```

### 3. تشغيل المشروع للتطوير
```bash
# استخدام التكوين المبسط
npx vite --config vite-simple.config.ts --port 3141 --host 0.0.0.0

# أو استخدام ملف index-new.html مباشرة
npx vite serve . --port 3141 --host 0.0.0.0
```

### 4. الوصول للتطبيق
افتح المتصفح وانتقل إلى: `http://localhost:3141`

## بيانات تسجيل الدخول التجريبية

### مدير النظام
- البريد الإلكتروني: `admin@gov.sa`
- كلمة المرور: `admin123`

### مدير الفرع
- البريد الإلكتروني: `branch@gov.sa`
- كلمة المرور: `branch123`

### موظف
- البريد الإلكتروني: `employee@gov.sa`
- كلمة المرور: `employee123`

### مستفيد
- البريد الإلكتروني: `beneficiary@gov.sa`
- كلمة المرور: `beneficiary123`

## بناء المشروع للإنتاج

```bash
# بناء المشروع
npx vite build --config vite-simple.config.ts

# معاينة البناء
npx vite preview --port 3141
```

## هيكل المشروع المحسن

```
src-new/
├── app/                    # إعدادات التطبيق الرئيسية
│   ├── store/             # إدارة الحالة (Redux)
│   ├── providers/         # مقدمي الخدمات
│   └── App.tsx           # المكون الرئيسي
├── shared/                # المكونات والخدمات المشتركة
│   ├── components/        # مكونات UI موحدة
│   ├── services/         # خدمات مشتركة
│   ├── hooks/            # React Hooks مخصصة
│   └── utils/            # دوال مساعدة
├── features/             # ميزات التطبيق
│   ├── auth/             # نظام المصادقة
│   ├── admin/            # لوحة تحكم المدير
│   ├── branch/           # إدارة الفروع
│   ├── employee/         # لوحة تحكم الموظف
│   └── beneficiary/      # لوحة تحكم المستفيد
├── assets/               # الملفات الثابتة
├── config/               # ملفات التكوين
└── main.tsx             # نقطة الدخول
```

## الميزات الجديدة

### 1. مكونات موحدة
- `Button` - أزرار موحدة مع دعم التحميل
- `Input` - حقول إدخال محسنة
- `Modal` - نوافذ منبثقة عامة
- `StatusBadge` - عرض الحالات
- `ErrorMessage` - عرض الأخطاء
- `DetailsModal` - عرض التفاصيل العام

### 2. خدمات محسنة
- `authService` - خدمة مصادقة موحدة
- `TokenManager` - إدارة الرموز المميزة
- دوال `dateHelpers` محسنة

### 3. إدارة حالة محسنة
- Redux Toolkit مع أفضل الممارسات
- تقسيم منطقي للـ slices
- دعم TypeScript كامل

## استكشاف الأخطاء

### مشكلة في التبعيات
```bash
# حذف node_modules وإعادة التثبيت
rm -rf node_modules package-lock.json
npm install
```

### مشكلة في المنفذ
```bash
# استخدام منفذ مختلف
npx vite --config vite-simple.config.ts --port 3142
```

### مشكلة في التكوين
```bash
# استخدام التكوين الافتراضي
npx vite --port 3141
```

## الدعم والمساعدة

للحصول على المساعدة، راجع:
- `REFACTORING_REPORT.md` - التقرير الشامل
- `optimization-recommendations.md` - توصيات التحسين
- `performance-analysis.py` - تحليل الأداء

## الترخيص
هذا المشروع مخصص للاستخدام الحكومي الداخلي.

---
**تم تطوير هذا النظام بواسطة:** Manus AI  
**تاريخ الإنشاء:** 11 يوليو 2025  
**الإصدار:** 2.0.0 (محسن)

